# Writing — media

- `ky-an-anh-trang.mp4` — AI test shot for the short film "Kỳ Án Ánh Trăng", shown on the
  script card in the "From script to screen" section. Compressed from the original for the web.
- `ky-an-anh-trang-poster.webp` — poster frame shown before the clip plays.

To add a clip to another script card, drop the files here and fill in `clip` on that entry
in `scriptwriting.credits` (`src/content/site.ts`).
